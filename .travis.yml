language: python

matrix:
  include:
  - python: 2.7
  - python: 3.6
#  - python: 3.7
#    sudo: required
#    dist: xenial

install:
  - pip install -r requirements.txt
  - pip install -e .
  - wget --user=HITRAN --password=getdata -N https://www.cfa.harvard.edu/HITRAN/HITRAN2012/HITRAN2012/By-Molecule/Compressed-files/01_hit12.zip && unzip 01_hit12.zip -d /home/travis/build/pcubillos/pyratbay/tests
  - wget http://kurucz.harvard.edu/grids/gridp00odfnew/fp00k0odfnew.pck -P /home/travis/build/pcubillos/pyratbay/tests
  - cd /home/travis/build/pcubillos/pyratbay && make

script: pytest tests/
