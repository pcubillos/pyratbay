[pytest]
markers=
    sort: execute tests sorted by the 'order' keyword argument. Execute all other unmarked tests after, in their default order.

filterwarnings =
    ignore:Importing from numpy.testing.decorators:DeprecationWarning
    ignore:the imp module is deprecated


addopts = --show-capture=no
